{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "render",
            "type": "shell",
            //"command": "echo ${workspaceFolder}",
            //"command": "pip3 show manim",
            "command": "python3 -m manim ${file} CreateCircle -s",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}